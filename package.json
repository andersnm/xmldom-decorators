{
  "name": "root",
  "private": true,
  "devDependencies": {
    "blerf": "file:./packages/blerf"
  },
  "dependencies": {},
  "scripts": {
    "preinstall": "cd packages/blerf && npm install && npm run build",
    "build": "blerf run build",
    "test": "blerf run test",
    "install": "blerf install",
    "pack": "blerf pack"
  }
}
